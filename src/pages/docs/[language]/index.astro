---
export async function getStaticPaths() {
  const langs = ['en', 'th', 'fr', 'ar']  // keep in one place if you like
  return langs.map((language) => ({ params: { language } }))
}

// …your existing frontmatter (getAllDocs, etc.)
---
