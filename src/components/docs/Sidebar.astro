---
import TOC from './TOC.astro'

const { headings = [] } = Astro.props
const items = Array.isArray(headings) ? headings : []
const hasItems = items.length > 0
---
{hasItems && (
  <aside class="hidden w-full max-w-[14rem] flex-shrink-0 lg:block">
    <TOC headings={items} />
  </aside>
)}
